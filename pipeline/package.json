{
  "name": "recordly-ai-pipeline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "recordly-ai: automated demo-video pipeline (repo ingest -> crawl -> record -> derive zoom/cursor -> export).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
